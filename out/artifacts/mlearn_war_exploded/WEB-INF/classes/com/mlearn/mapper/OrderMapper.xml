<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mlearn.mapper.OrderMapper">

    <resultMap id="orderMapper" type="order">
        <id property="id" column="id"></id>
        <result property="ordernum" column="ordernum" />
        <result property="buyerId" column="buyerId" />
        <result property="createdate" column="createdate" />
        <result property="state" column="state" />
        <result property="totalprice" column="totalprice" />
    </resultMap>

    <select id="findAll" resultMap="orderMapper">
        SELECT id, ordernum, buyerId, createdate, state, totalprice FROM orders
    </select>


    <select id="findBySate" resultMap="orderMapper">
        SELECT id, ordernum, buyerId, createdate, state, totalprice FROM orders WHERE state = #{state}
    </select>


    <update id="updateState">
         UPDATE orders SET state = #{state} WHERE orders.id = #{id}
    </update>

    <select id="selectByOrderNumber" parameterType="int" resultMap="com.mlearn.mapper.OrderItemMapper.orderItemMapper">
      select item.*, o.*, b.*, u.id from orders o
      INNER JOIN orderitems item
      on o.ordernum = item.orderId
      INNER JOIN books b
      on b.id = item.bookId
      INNER JOIN user u
      on u.id = o.buyerId
      where o.ordernum = #{orderNumber}
    </select>

</mapper>