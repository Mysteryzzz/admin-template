<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mlearn.mapper.BookMapper">

    <resultMap  id="bookMapper" type="book">
        <id property="id" column="id"></id>
        <result property="bookname" column="bookname" />
        <result property="price" column="price" />
        <result property="img" column="img" />
        <result property="stock" column="stock" />

    </resultMap>

    <select id="findAll" resultMap="bookMapper">
        SELECT id, bookname, price, img, stock FROM books WHERE state = 1
    </select>

    <update id="deleteById">
        UPDATE books SET state = 0 WHERE id = #{id}
    </update>

    <select id="findBookInOrder" resultType="Integer">
        SELECT count(*) FROM orderitems WHERE bookId = #{id}
    </select>

    <update id="update" parameterType="book" useGeneratedKeys="true" keyProperty="id">
        UPDATE books SET bookname = #{bookname}, price = #{price}, img = #{img}, stock = #{stock}
        WHERE id = #{id}
    </update>



</mapper>