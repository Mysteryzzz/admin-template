<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mlearn.mapper.OrderMapper">

    <resultMap id="orderMapper" type="order">
        <id property="id" column="id"></id>
        <result property="ordernum" column="ordernum" />
        <result property="buyerId" column="buyerId" />
        <result property="createdate" column="createdate" />
        <result property="state" column="state" />
        <result property="totalprice" column="totalprice" />
    </resultMap>

    <select id="findAll" resultMap="orderMapper">
        SELECT id, ordernum, buyerId, createdate, state, totalprice FROM orders
    </select>


    <update id="updateState">
         UPDATE orders SET state = #{state} WHERE orders.id = #{id}
    </update>


</mapper>